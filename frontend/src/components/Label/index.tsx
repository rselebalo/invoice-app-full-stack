import styled from 'styled-components';

const StyledLabel = styled.div`
  padding: 15px 0;
  font-weight: bold;
  color: ${(props) => props.theme.button1.default.background};
`;
export default StyledLabel;
